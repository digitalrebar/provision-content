---
Name: "govc/debug"
Description: "Enables GoVC debugging, separate from rs-debug-enable control."
Documentation: |
  If set to ``true``, then additional debug output and behaviors will occur with the ``govc`` command usage.

  By default this value is set to ``falsse``.

Schema:
  type: "boolean"
  default: false
Meta:
  color: "yellow"
  icon: "terminal"
  title: "RackN Content"
