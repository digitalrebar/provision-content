---
Name: "govc-commands"
Description: "Run govc commands in GoVMOMI enabled context container."
Documentation: |
  This Stage runs a _govc_ command specified by the ``govc/commands``
  Parameter in the _govc_ context container.  If not command is
  specified, the default action is to simply run ``govc about`` against
  the remote specified ESXi or vCenter API service.

Tasks:
  - "context:govc"
  - "govc-commands"
Meta:
  icon: "terminal"
  color: "black"
  title: "RackN Content"
