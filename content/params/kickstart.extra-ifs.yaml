---
Name: "kickstart/extra-ifs"
Description: "Extra interfaces to configure during kickstart builds"
Documentation: |
  Extra interfaces to configure during kickstart-based builds.  This is only
  required if you are building an instance on a VM with nonstandard
  networking.

Schema:
  type: "array"
  items:
    type: "string"
  default: []

Meta:
  icon: "time"
  color: "blue"
  title: "Digital Rebar Community Content"
