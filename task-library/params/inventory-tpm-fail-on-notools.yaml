---
Name: "inventory/tpm-fail-on-notools"
Description: "Should the inventory tasks fail if no TPM tools are present"
Documentation: |
  If set to true, the system fail if the TPM tools are not present.

  Defaults to `false`.

Schema:
  type: "boolean"
  default: false
Meta:
  color: "black"
  icon: "expand"
  title: "RackN Content"
