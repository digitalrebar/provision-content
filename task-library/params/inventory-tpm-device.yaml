---
Name: "inventory/tpm-device"
Description: "The device to use to query the TPM."
Documentation: |
  The device to use to query the TPM.

  Defaults to `/dev/tpm0`

Schema:
  type: "string"
  default: /dev/tpm0
Meta:
  color: "black"
  icon: "expand"
  title: "RackN Content"
