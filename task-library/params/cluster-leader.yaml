---
Name: "cluster/leader"
Description: "Flag indiciating that machine is a cluster leader"
Documentation: |
  Added during cluster-setup to flag a cluster machines as leader.

  Cluster Leader = machines in cluster defined as leaders
  Cluster Manager = DRP contexts in cluster that coordinate activity

  For operational guidelines, see :ref:`rs_cluster_pattern`
Schema:
  type: boolean
  default: false
Meta:
  type: "config"
  color: "blue"
  icon: "chess knight"
  title: "RackN Content"
