---
Name: "inventory/integrity"
Description: "Fail if inventory been changed"
Documentation: |
  Allows operators to compare new inventory/data
  to stored inventory/data on the machine.
  If true and values not match (after first run) then Stage will fail
Schema:
  type: boolean
  default: false
Meta:
  color: "green"
  icon: "handshake outline"
  title: "RackN Content"
