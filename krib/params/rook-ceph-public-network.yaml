---
Name: "rook/ceph-public-network"
Description: "Network (in CIDR format) rook ceph will use for public traffic - make this the same as the Kubernetes control-plane network"
Documentation: |
  This should be set to match the kubernetes "control plane" network on the nodes
Schema:
  type: "string"
Meta:
  color: "blue"
  icon: "chess rock"
  title: "Community Content"
