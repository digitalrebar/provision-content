---
Name: "krib/externaldns-container-image"
Description: "Custom container image to use for ExternalDNS"
Documentation: |
  Allows operators to optionally override the container image used in the
  ExteranlDNS deployment. Possible use case would be pre-prepared images pushed to a 
  local trusted registry
  
Schema:
  type: "string"
Meta:
  color: "blue"
  icon: "setting"
  title: "Community Content"
