---
Name: "krib/rook-ceph-container-image-daemon-base"
Description: "Custom container image to use for rook ceph daemon-base"
Documentation: |
  Allows operators to optionally override the container image used in the
  rook-ceph deployment. Possible use case would be pre-prepared images pushed to a
  local trusted registry

Schema:
  type: "string"
Meta:
  color: "blue"
  icon: "setting"
  title: "Community Content"
