---
Name: "krib/metallb-container-image-controller"
Description: "Custom container image to use for MetalLB controller"
Documentation: |
  Allows operators to optionally override the container image used in the
  MetalLB controller deployment. Possible use case would be pre-prepared images pushed to a
  local trusted registry

Schema:
  type: "string"
Meta:
  color: "blue"
  icon: "setting"
  title: "Community Content"
