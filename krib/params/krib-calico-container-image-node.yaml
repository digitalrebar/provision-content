---
Name: "krib/calico-container-image-node"
Description: "Custom container image to use for Calico node"
Documentation: |
  Allows operators to optionally override the container image used in the
  Calico deployment. Possible use case would be pre-prepared images pushed to a
  local trusted registry

Schema:
  type: "string"
Meta:
  color: "blue"
  icon: "setting"
  title: "Community Content"
