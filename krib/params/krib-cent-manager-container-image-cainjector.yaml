---
Name: "krib/cert-manager-container-image-cainjector"
Description: "Custom container image to use for cert-manager cainjector"
Documentation: |
  Allows operators to optionally override the container image used in the
  cert-manager deployment. Possible use case would be pre-prepared images pushed to a
  local trusted registry

Schema:
  type: "string"
Meta:
  color: "blue"
  icon: "setting"
  title: "Community Content"
