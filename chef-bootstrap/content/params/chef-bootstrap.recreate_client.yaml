---
Name: "chef-bootstrap/recreate_client"
Description: "Recreate client and node objects if they already exist on the Chef Server"
Documentation: |
  If ``true`` we delete client and node if they already exist on the Chef Server, before creating them.
Schema:
  type: "boolean"
  default: false
Meta:
  color: "orange"
  icon: "repeat"
  title: "Digital Rebar Community Content"