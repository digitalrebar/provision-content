---
Name: proxmox/lab-drp-external-interface
Description: Sets the External interface that the DRP Endpoint uses (defaults to eth0)
Documentation: |
  The DRP Endpoint virtual machine external interface that should be publically accessible
  to the operators.  This interface should ultimately obtain an IP address that is routable
  to for the DRP operators.

Meta:
  color: blue
  icon: database
  title: Digital Rebar Community Content
Schema:
  default: eth0
  type: string
Secure: false

