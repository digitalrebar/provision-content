---
Name: proxmox/storage-device
Description: The disk on the Proxmox VE server for storage volumes.
Documentation: |
  This param is used to define the disk that the base storage volume
  will be created on.  It defaults to ``/dev/sdb`` if not otherwise
  defined.

Meta:
  color: blue
  icon: database
  title: Digital Rebar Community Content
Schema:
  default: "/dev/sdb"
  type: string
Secure: false

